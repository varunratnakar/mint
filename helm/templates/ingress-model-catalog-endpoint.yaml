apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: model-catalog-endpoint-{{ include "mint.prefix" . }}-ingress
  namespace: {{ .Release.Namespace }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.tls }}
  {{- if .enabled }}
  tls:
    - hosts:
      - endpoint.models.{{ $.Values.hostname }} 
      secretName: model-catalog-endpoint-{{ include "mint.prefix" $ }}-tls-secret
  {{- end }}
  {{- end }}
  rules:
    - http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ include "mint.prefix" . }}-model-catalog
              port:
                number: 3030
      {{- if .Values.hostname }}
      host: endpoint.models.{{ .Values.hostname }}
      {{- end }}
